# 背单词知识卡片 Skill — Agent 开发指南

## 项目定位

背单词知识卡片 Skill 是一个将用户提供的课程文案、课堂转写、笔记或学习材料，转化为可复习的英语词汇知识卡片与学习资料的工具。它从内容中提炼学习框架、单词、词族、课堂助记过程和复习任务，并生成可导航的文档与关系图。

## 核心能力

### 1. 知识提炼

根据用户输入的文案，识别并整理：

- 课程主题、目标与结论；
- 核心概念、方法论、步骤和例子；
- 概念之间的前置关系、从属关系、对比关系与因果关系；
- 可执行的学习顺序、复习任务和自检问题；
- 转写中存在歧义、错词或需要用户核对的内容。

输出至少包含一个知识 schema 记录；复杂内容还应生成方法论总结、学习路径或待核对项，并由 Web 项目渲染为可阅读页面。

### 2. 单词卡片生成

当文案讲解英语单词、词组、词根、词缀或词族时，为每个可明确识别的词汇创建单独的 schema/words JSON 卡片记录。

每张卡片至少包含：

- 单词/词组和基本中文释义；
- 课程中的原始记忆方法：保留讲解者的提问、拆分顺序、声音模仿、形象画面、故事或词族推导；
- 可复现的复习动作：用户应如何朗读、想象、拆分或口述；
- 自检题与间隔复习建议；
- 使用提醒：明确课程助记法不等同于严格词源、词典释义或语法规则。

仅在英文拼写和含义足够可信时建立正式卡片。对于语音转写不清、拼写可能错误或含义无法确认的内容，写入对应 knowledge JSON 的 uncertainItems 字段，不要将其作为确定知识固化。

### 3. 文档与关系图生成

根据提炼结果，生成可供 Web 直接读取的 JSON Schema，例如：

~~~text
schema/
├── knowledge/
│   └── lesson-id.json
├── words/
│   ├── word-a.json
│   └── word-b.json
└── relationships/
    └── lesson-id.graph.json

web/
└── 读取 schema 并渲染图谱与单词卡片
~~~

relationships 目录应保存图谱节点与边的 JSON 数据；web 项目据此渲染关系图，并至少表达以下一种关系：

- 课程总主题到核心概念的层级；
- 方法论的执行顺序；
- 单词词族的扩展关系；
- 单词、记忆画面与词义之间的关联。

图谱 JSON 的节点应有 id、type、label；边应有 source、target、type。Web 项目依据 JSON 绘制图论图、支持搜索定位并展示选中单词卡片。

## 工作流程

1. 阅读用户给出的全部文案；不要把语音转写错误当成事实。
2. 提取课程结构、明确词汇、记忆方法与不确定项。
3. 先生成 schema/knowledge 中的课程记录和 schema/relationships 中的图谱，再生成 schema/words 中的单词卡片。
4. 每个单词卡片单独成 JSON 文件；图谱中的每个单词节点必须能定位到同 id 的卡片。
5. 根据内容复杂度写入学习任务、复习建议和待核对项。
6. 验证 JSON 语法、节点与边引用、卡片数量以及 Web 读取路径的一致性。

## 质量标准

- **忠实**：课堂中的记忆过程应按原意还原，不要只写抽象概括。
- **可用**：学习者无需回看原文，也能按照卡片复现记忆方法。
- **可追溯**：对不确定内容显著标记，并说明需要核对的原因。
- **可导航**：总览、索引、卡片和关系图之间应有清晰链接。
- **不过度断言**：避免将助记联想写成语言学事实；词义、发音、词性和词源以可靠词典为准。
